/**
 * Alert — alias de compatibilidade para o componente Message GovBR.
 * Use `Message` diretamente para acesso completo à API.
 */
export { Message as Alert, type MessageProps as AlertProps } from '../Message/Message'
